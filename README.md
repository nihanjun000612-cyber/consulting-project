# consulting-project

## Added resources

- `gvhd_binary_classification.R`: 以移植前变量为自变量，基于 `agvhd24` 与 `agvhd34` 构建 `y3`（轻/中/重）并最终落地为二分类 `y_bin`（重度 vs 非重度）的统计建模脚本。
- `SYSTEM_PROMPT_CN.md`: 可复用的“研究生水平统计咨询”系统提示词模板。
